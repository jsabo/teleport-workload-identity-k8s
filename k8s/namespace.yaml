apiVersion: v1
kind: Namespace
metadata:
  name: teleport-wi
  labels:
    # The issuer runs privileged with hostPID; clusters that enforce Pod Security
    # Admission (Talos does by default) need the namespace opted in.
    pod-security.kubernetes.io/enforce: privileged
